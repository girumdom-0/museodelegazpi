# Supabase Dashboard -> Project Connect -> URL and Publishable key.
# The publishable key is safe in browser code when RLS is enabled (see the migration).
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
